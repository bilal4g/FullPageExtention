/* Deprecated. Math extraction is now done on-device from the page DOM
 * (see studio-bridge.js -> ssExtractMathInject). No API is used. This file
 * is intentionally left as a no-op so older cached references do not 404. */
